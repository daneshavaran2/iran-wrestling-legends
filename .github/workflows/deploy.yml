name: Deploy

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      artifact-id: ${{ steps.upload.outputs.artifact-id }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Upload build artifacts
        id: upload
        uses: actions/upload-artifact@v4
        with:
          name: production-build
          path: build/
          retention-days: 30

  # Deploy via SSH (uncomment and configure secrets to enable)
  # deploy-ssh:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
  #
  #   steps:
  #     - name: Download build artifacts
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: production-build
  #         path: build/
  #
  #     - name: Deploy to server via SSH
  #       uses: appleboy/scp-action@v0.1.7
  #       with:
  #         host: ${{ secrets.SSH_HOST }}
  #         username: ${{ secrets.SSH_USER }}
  #         key: ${{ secrets.SSH_KEY }}
  #         source: "build/*"
  #         target: ${{ secrets.SSH_PATH }}
  #         strip_components: 1

  # Deploy to Docker Registry (uncomment and configure secrets to enable)
  # deploy-docker:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Download build artifacts
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: production-build
  #         path: build/
  #
  #     - name: Login to Docker Hub
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKER_USERNAME }}
  #         password: ${{ secrets.DOCKER_PASSWORD }}
  #
  #     - name: Build and push Docker image
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         push: true
  #         tags: |
  #           ${{ secrets.DOCKER_USERNAME }}/wrestling-museum:latest
  #           ${{ secrets.DOCKER_USERNAME }}/wrestling-museum:${{ github.sha }}

  # Deploy to GitHub Pages (uncomment to enable)
  # deploy-pages:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
  #   permissions:
  #     pages: write
  #     id-token: write
  #
  #   environment:
  #     name: github-pages
  #     url: ${{ steps.deployment.outputs.page_url }}
  #
  #   steps:
  #     - name: Download build artifacts
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: production-build
  #         path: build/
  #
  #     - name: Setup Pages
  #       uses: actions/configure-pages@v4
  #
  #     - name: Upload to GitHub Pages
  #       uses: actions/upload-pages-artifact@v3
  #       with:
  #         path: build/
  #
  #     - name: Deploy to GitHub Pages
  #       id: deployment
  #       uses: actions/deploy-pages@v4

  # Deploy to Liara (uncomment and configure LIARA_API_TOKEN secret to enable)
  # deploy-liara:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Download build artifacts
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: production-build
  #         path: build/
  #
  #     - name: Install Liara CLI
  #       run: npm install -g @liara/cli
  #
  #     - name: Deploy to Liara
  #       env:
  #         LIARA_API_TOKEN: ${{ secrets.LIARA_API_TOKEN }}
  #       run: |
  #         liara deploy --api-token $LIARA_API_TOKEN --detach
